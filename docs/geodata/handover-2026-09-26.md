# Geographic map feature handover — 2026-09-26

This is a preservation checkpoint after the instruction to stop feature development
and tests. The feature is **not ready to merge end to end**. Do not interpret an
open or GitHub-mergeable pull request as an acceptance pass. The original
September 26 audit plan is a local attachment on the prior machine; its
remaining acceptance work is summarized here.

## Remote code inventory

The pushed `codex/map-completion-integration` branch is at
`7b8ad8bea9254e8db4d5907e40b29830c7114bb2`. The same commit is the
pushed `codex/map-final-integration-base`. Earlier map foundations and
integration are retained in the existing stacked PRs, including #8–#44,
#61, and #62. The recent implementation and preservation PRs are:

| PR | Head | Base | Preserved work |
| --- | --- | --- | --- |
| [#63](https://github.com/Anko59/AoeWorld/pull/63) | `da654044fefa0ae39eeb73324710df8b3878c90a` | final integration base | Bounded fuzz corpus lifecycle; nightly still unproven |
| [#64](https://github.com/Anko59/AoeWorld/pull/64) | `17eaaa84e98043e12595c1b8a7e53ed5cc97dbe8` | final integration base | Canvas/WebGPU terrain depth and overlap correctness |
| [#65](https://github.com/Anko59/AoeWorld/pull/65) | `a3b9a5bdee0bee54867b6274dc6755a7329bacec` | final integration base | Ordinary creator journey and fixed source matrix |
| [#66](https://github.com/Anko59/AoeWorld/pull/66) | `6de17103fce86558dfc270fb57c9a13a14e444fd` | final integration base | Production HYDE area allocation |
| [#67](https://github.com/Anko59/AoeWorld/pull/67) | `e32c167cf45f1d65f26f7fda105d872910e29777` | #66 | Page-window HYDE history and coverage provenance |
| [#68](https://github.com/Anko59/AoeWorld/pull/68) | `193b95c8b9728ba0dd4ef8348da5845c70615318` | final integration base | Source-backed geographic navigation and scale qualification |
| [#69](https://github.com/Anko59/AoeWorld/pull/69) | `5e5b9c4694f8740d007a728b75048dd633c9594b` | `codex/source-visual-base` | Source-backed visual journey and verification tools |
| [#70](https://github.com/Anko59/AoeWorld/pull/70) | `32e1248482d0a10a4f29a0a90ad621fcce7703ca` | #66 | Coherent modeled water and geographic water corrections |
| [#71](https://github.com/Anko59/AoeWorld/pull/71) | `2fddffada57fcbf5daccfeba79e65b6ef96eec4f` | #72 | Geographic vegetation corrections; preservation draft |
| [#72](https://github.com/Anko59/AoeWorld/pull/72) | `d9fc3263720754cc088dfdf72b6eb5fd4f1f60f8` | final integration base | Partial integration checkpoint and status; preservation draft |
| [#73](https://github.com/Anko59/AoeWorld/pull/73) | `f1b25e113255ef37af24aa029fc80033c9065336` | #67 | Bounded geographic HYDE corrections, independent historical axis, compact coverage wire; preservation draft |

PR #72 contains earlier commits from #63–#67, but predates the latest #63
commit and does not contain #68–#71 or #73. PR #69 is based on a source visual
base that contains earlier fuzz, rendering, and creator commits. Reconcile by
commit identity and content; avoid merging the same feature twice. The final
integration checkpoint is intentionally based on the pushed map stack rather
than `dev`, which is many stacked commits behind. PRs #63, #67–#73 are drafts
where final verification or integration is outstanding.

## What was observed before the stop

- HYDE production allocation and historical streaming had focused tests and
  Dockerized checks on their feature revisions. The independent historical
  axis and compact coverage wire in #73 were not tested together at its head.
  A full 64×64 history coverage page may otherwise exceed the 128 KiB page
  limit; verify the new compact encoding and legacy decoding before merging.
- The navigation workload in #68 reports five ordinary Sahara orders of about
  20 km each, about 100,024.85 m and 666,992 ticks, deterministic replay, page
  reads, and bounded work. The unchanged Paris start remains in a roughly
  2,610-tile component with 418 tree boundary blockers; its direct 20 km
  search reaches `SearchLimit`. Source-backed 512, 16,384, and 50,000 cases
  and a sparse 262,144 contract were recorded, but final-recipe regeneration
  remains outstanding. Synthetic workloads do not qualify RTS performance.
- Water #70 models connected lake levels and directionless river surface from
  the DEM, with junction ties and correction precedence. Its latest river
  surface fallback, all-lake component fallback, and file split were not
  formatted or tested after the stop. The current adapter discards
  HydroRIVERS `HYRIV_ID`, `NEXT_DOWN`, and `DIST_DN_KM`, so directed flow is
  unknown even though the source has topology fields. No ordinary-worker
  lake/river/coast regeneration or visual inspection followed these edits.
- Rendering #64 addressed constructed overlap scenes. Visual qualification
  #69 has focused unit and browser checks after its final code edits, but its
  full preflight and refreshed 11-case browser captures remain outstanding.
  The revised Nile Delta coast case has ocean evidence but no inland channel;
  do not call it a demonstrated estuary.
- Creator #65 contains an ordinary source-backed journey and fixed matrix,
  but the final combined recipe, water/history fields, and post-change visual
  results have not been demonstrated end to end.
- Fuzz #63 passed hooks, 560 native unit tests, seven smoke targets, and
  preflight at its head. The earlier nightly failed the fixed 16,384 active
  corpus quota; the newer campaign was interrupted at user direction, exit
  130, before completion. Two clean-revision full nightly passes remain due.
- Vegetation #71 passed `make build`, `make hooks-install`,
  `make hooks-check`, and `make test-unit` (586) before the stop. Its preflight
  and source-backed regenerated package evidence remain due.

The latest preservation commits for #70–#73 used a one-time
`core.hooksPath=/dev/null` for commit/push because the user explicitly stopped
further tests. PR #68 also used this preservation exception. Required
formatting, focused checks, preflight, and final-revision gates must be run
before these drafts can become ready to merge. The integration branch's last
full preflight was interrupted by an OS `WouldBlock` error during concurrent
suites; it has not passed at #72's head.

## Remaining feature work

1. Reconcile #63–#71 and #73 into a single final integration branch, resolve
   conflicts, and verify recipe/preprocessing/package compatibility and legacy
   fingerprints. Ensure the ordinary worker invokes the corrected historical,
   vegetation, and water paths. Decide whether #73's compact history coverage
   belongs in #67 before allowing full-size page publication.
2. Run a small offline production fixture for historical conservation,
   partial selections, odd reductions, unknown coverage, and the independent
   historical axis. Regenerate a real fixed-region package and record changed
   page roots, source locks, provenance, and package hash. Verify full-size
   coverage page bounds and legacy decoding.
3. Integrate HydroRIVERS topology if directed profiles are required by the
   selected source region; otherwise disclose unknown direction. Test
   cross-page lakes/rivers, junctions, correction precedence, and shared
   renderer/movement water classification. Regenerate ordinary-worker lake,
   river, and coast cases.
4. Rerun navigation on the final generated recipe. Preserve the unchanged
   Paris failure, demonstrate the predetermined traversable long route and
   page residency, and report source-backed scale cases with work and memory
   bounds. Do not change start clearance or search caps just for a pass.
5. Run the complete ordinary creator journey on the final package: preview,
   progress, verify, activate, reload, server restart/reconnect, persisted
   resources, and offline unseen chunk. Refresh the fixed geographic matrix
   and both Canvas/WebGPU captures; inspect high relief, eviction/return,
   water, banks, forests, resources, and sprite contacts. Correct any visual
   anomalies found.
6. Run two full nightly fuzz campaigns without quota abort or discarded crash
   artifacts. At the exact final revision run the focused gates and
   `make hooks-install`, `make hooks-check`, `make preflight`, `make map-test`,
   `make browser-check`, `make test-wasm`, `make test-e2e`, `make coverage`,
   `make coverage-check`, `make fuzz-smoke`, `make fuzz-nightly`,
   `make perf-ci`, and `make map-perf`, plus private-art and source-backed
   qualification. Report revision, command, result, and hardware limits.
7. Obtain required CI on a final PR against the supported merge base and
   make only fully qualified PRs ready to merge. No current PR demonstrates
   the complete final-revision acceptance contract.

## Local data and machine cleanup

GitHub PRs preserve tracked code and documents, **not ignored data**. Before
removing the machine, copy or intentionally discard these separately:

- `.cache/geodata` (about 7.7 GiB): cached source acquisition; original
  geodata cannot be added to Git under the working agreement.
- `.cache/map-completion` (about 196 MiB): source qualification reports and
  packages, including Sahara/Paris evidence and scale samples.
- `local-assets/maps-matrix` and other `local-assets`: generated map packages
  and private game art; keep original/private art outside Git and images.
- `reports` (about 43 MiB): prior gate, visual, and fuzz reports. Several are
  revision-bound or stale and are not final acceptance evidence.
- `fuzz` (about 511 MiB locally): ignored active/archived corpus and crash
  artifacts; tracked original seeds remain in Git, but discovered inputs and
  archives require a separate copy if they are to be retained.

Two old dirty worktrees were audited read-only and contain superseded
prototypes, so no archival PR is needed:

- `AoeWorld-map-geodata` at `04464752609a4bcd74a7d4d4c5943f20233dac65`
  has eight modified and five untracked files. Its request change is
  byte-identical to the current tracked `crates/map/src/request.rs`
  (`SHA-256 431beb289ac8ff21163ace82209b75ccba9b0aee0787d9c488b80f759bc015ad`).
  Directory publication, page verification, six-layer roots, and HYDE
  nodata handling are superseded by tracked schema-9 and production HYDE
  modules in the pushed integration and #66/#67.
- `AoeWorld-map-schema8-fixture` at
  `0ca2cd174ef397b20fe5538441bf384988942a7f` has a one-off fixture
  writer and fixture. The fixture is byte-identical to the current tracked
  file (`SHA-256 5db9b3fd7a000f5636d4f975e7fd7137933ed74f8bf9d5926350281680d6c478`),
  already consumed by the legacy schema/hash test. The local helper only
  regenerated that fixture.

These old worktrees are ancestors of the pushed final integration base by
85 and 59 commits respectively. Their dirty prototypes can be removed after
this content audit. A clean worktree and an open PR preserve tracked
implementation, but do not imply ignored evidence is recoverable from GitHub.
