# 0001: Integer simulation and sparse chunks

Use seeded integer motion, stable entity order, contiguous entity storage, and
a sparse chunk index. This supports reproducible replays and sparse worlds
without selecting a general ECS before gameplay requirements are known.
