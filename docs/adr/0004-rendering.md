# 0004: WebGPU browser rendering

Use Rust/WASM with wgpu's WebGPU backend and instanced sprites. Surface
unsupported capabilities explicitly. The initial browser client is single
threaded and has no WebGL fallback.
