# WASM and rendering

Read [architecture](../../docs/architecture.md), [testing](../../docs/testing.md), and the scoped instructions in `crates/client/AGENTS.md` and `crates/rendering/AGENTS.md`. Check WebGPU capability failures and resource counters. Run `make build-wasm`, `make test-e2e`, and `make perf-ci` when rendering work changes.
