# Protocol

Read [transport ADR](../../docs/adr/0002-transport.md) and `crates/protocol/AGENTS.md`. Preserve versioned bounded decoding and compatibility fixtures. Run `make preflight` and `make test-e2e`; include malformed, stale-version, and reconnect cases for wire changes.
