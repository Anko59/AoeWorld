# Server changes

Keep environment reads in `config.rs`, bound client work, and clean up failed
connections. Read [architecture](../../docs/architecture.md) and run focused
integration coverage plus `make preflight`.
